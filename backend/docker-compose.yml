version: '3.8'

services:
  # node-server:
  #   build:
  #     dockerfile: ./Dockerfile
  #   restart: always
  #   ports:
  #     - 3000:3000

  mongo:
    image: mongo:latest
    restart: always
    ports:
      - 27017:27017
    volumes:
      - ./db:/data/db:rw
