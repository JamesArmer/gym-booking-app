version: '3.1'

services:
  mongo:
    image: mongo:latest
    restart: always
    ports:
      - 27017:27017
    volumes:
      - ./db:/data/db:rw

  mongo-express:
    image: mongo-express:latest
    restart: always
    ports:
      - 8080:8081
